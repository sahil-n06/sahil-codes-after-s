#include<stdio.h>
#include<string.h>
#include<stdlib.h>
void sort(int n,char arr[][])
{
  int i,j;
  char temp[20];
  for(i=0;i<n;i++)
  {
    for(j=0;j<n;j++)
    {
      if(strcmp(name[i],name[j+1])>0){
        strcpy()
      }
    }
  }
}
void main()
{
  int n;
  char arr[20][20];
  printf("Enter the number of stud you want to insert : ");
  scanf("%d",&n);
  for(i=0;i<n;i++){
  printf("enter the name :");
  scanf("%s",arr);}
  sort(n,arr);
}
